pool: Azure Pipelines 

steps:
  - checkout: self

  - task: SnykSecurityScan@1
    inputs:
      serviceConnectionEndpoint: 'Snyk CI'
      testType: 'app'
      monitorWhen: 'always'
      failOnIssues: false
      

